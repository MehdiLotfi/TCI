@model IEnumerable<TCI.Model.OcdfRoom>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            نام
        </th>
        <th>
            OcdfRoomId
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Name
            </td>
            <td>
                ...
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.OcdfRoomId }) |
                @Html.ActionLink("Details", "Details", new { id = item.OcdfRoomId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.OcdfRoomId })
            </td>
        </tr>
    }

</table>
